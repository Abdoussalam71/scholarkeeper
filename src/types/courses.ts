
export interface CourseData {
  id: string;
  name: string;
  description: string;
  teacherId: string;
  teacherName: string;
  schedule: string;
  duration: string;
  maxStudents: number;
  currentStudents: number;
}
